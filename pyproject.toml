[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "pythondependencytest"
version = "0.1.0"
description = "Python Dependency Test"
readme = "README.md"
requires-python = ">=3.11"